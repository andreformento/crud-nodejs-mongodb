# CRUD, Express and MongoDB

## To run
### Local (development)
* `npm install`
* `npm run dev`
* Access: `http://localhost:3000`

### Remote (production)
* `npm install`
* `npm start`

### Docker
* `docker-compose build && docker-compose up`

## Requirements
* npmjs
* NodeJS
* MongoDB
* Docker (optional)
